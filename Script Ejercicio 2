function paroimpar()
{
    var numero = document.getElementById("texto").value;
    var text = parseInt(numero);

if (numero%2 == 0) {
    alert("Es numero par");
}
else {
    alert("Es numero impar");
}}
